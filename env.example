# React build-time environment variables (CRA)
# These are baked into the build output during `npm run build`.
# After changing, redeploy to rebuild.

REACT_APP_API_BASE_URL=https://YOUR-BACKEND.azurewebsites.net
